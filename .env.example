DISCORD_TOKEN=your_bot_token_here
CLIENT_ID=your_discord_application_client_id_here

# Optional. Defaults to data/database.json.
# Render uses /data/database.json through render.yaml.
# Railway uses RAILWAY_VOLUME_MOUNT_PATH automatically when DATABASE_PATH is omitted.
DATABASE_PATH=data/database.json
